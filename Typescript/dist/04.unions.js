"use strict";
let union;
union = 27;
union = 'Lockheart';
console.log(union);
const f = (z) => {
    if (typeof z === 'string') {
        console.log(z.toUpperCase());
    }
    else {
        console.log(z);
    }
};
